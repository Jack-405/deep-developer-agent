"""通用 MCP 集成工具使用规范（适用于所有通过 MCP 挂载的外部能力）。

后续新增其他 MCP（数据库、CI、外部服务等）时，本规范保持通用，
具体工具的引导写在对应的 section 模块里。
"""

MCP_PROMPT = """
MCP 集成工具：

本环境可能通过 MCP 协议挂载外部集成工具（编辑器、运行时、外部服务等）。
这些工具不属于常规文件/命令工具，调用它们会带来额外开销。

使用原则：

- 静态优先：能通过读文件、搜索或已有信息判断的问题，不要调用 MCP 工具。
- 按需调用：只在确需操作外部系统时才调用；调用前明确本次调用的目的。
- 结果最小化：优先选择返回体积小的工具；有分页、深度、数量类参数的工具，
  先使用较小的值，避免一次拉取全量数据。
- 失败处理：工具失败时先分析原因再决定下一步，不要原样重试。
"""
